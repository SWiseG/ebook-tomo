dbs:
  - path: /data/db/ebook.db
    replicas:
      - type: s3
        endpoint: ${R2_ENDPOINT}
        bucket: ${R2_BUCKET}
        path: sqlite/ebook
        access-key-id: ${R2_ACCESS_KEY}
        secret-access-key: ${R2_SECRET}
        sync-interval: 10s
